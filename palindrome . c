#include  <stdio.h>
int main()  {
    int n;
    if (scanf("%d" , &n)  != 1) return 0;
    long long arr[n];
    for (int i = 0; i < n ; i++) {
        scanf("%lld" , &arr[i]);
    } 
    int is_palindrome = 1;
    for (int i = 0; i < n / 2; i++) {
        if (arr[i]  != arr[n - 1 -i]) }
            is_palindrome = 0;
            break;                     
                            if (is_palindrome) {
                                printf("YES\n");
                                } else {
                                   printf ("NO\n");
                        return 0;
                        }

    
